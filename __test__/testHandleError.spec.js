import { handleError } from "../src/client/js/handleError"

describe("Testing the submit functionality", () => {
    test("Testing the handleSubmit() function", () => {
        //test that handleError() is defined
        expect(handleError).toBeDefined();
})});